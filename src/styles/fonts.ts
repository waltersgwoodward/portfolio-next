import { Alegreya, Sacramento } from "next/font/google";

// define your variable fonts
const alegreya = Alegreya({ subsets: ["latin"] });
const sacramento = Sacramento({ weight: "400", subsets: ["latin"] });

export { alegreya, sacramento };
