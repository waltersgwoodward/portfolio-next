import Form from "./form";
import Nav from "@/components/nav-bar";
import { Fragment } from "react";

const Home = () => {
  return (
    <Fragment>
      <Nav />
      <Form />
    </Fragment>
  );
};

export default Home;
